package com.safiej.bulletjournal.model.bullets

enum class TaskState {
    INCOMPLETE,
    COMPLETE,
    MIGRATED_COLLECTION,
    MIGRATED_FUTURE_LOG
    //Irrelevant marked as inactive in bullet (active=false)
}